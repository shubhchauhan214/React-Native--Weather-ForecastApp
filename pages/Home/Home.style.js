import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
    basic:{flex:2},
    searchbar:{flex:2},
    advanced:{flex:1},
    txt:{fontSize:30, color:"white", fontFamily: "Alata-Regular"},
});